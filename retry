#!/bin/sh -e

RETRIES="${RETRIES:-3}"
SLEEP="${SLEEP:-3}"

until "$@"; do
    [ "$RETRIES" -ge 0 ] || break
    echo "Retrying up to $RETRIES more times after sleeping ${SLEEP}s …"
    RETRIES=$((RETRIES-1))
done
[ $RETRIES -ge 0 ]
